Create a structured feature plan before coding.

Read `docs/architecture/overview.md` for current system state, then follow the instructions in `.claude/skills/plan-feature/SKILL.md` exactly.

Feature to plan: $ARGUMENTS

If the skill file is not found, create a plan at `docs/plans/active/[feature-name].md` with: Goal, Context, Approach, Tasks (checklist), API Changes, Data Model Changes, Infrastructure Changes, Acceptance Criteria, and Open Questions.
